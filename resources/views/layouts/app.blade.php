<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>@yield('title','Cash Flow') · Klinik Bustari</title><link rel="stylesheet" href="{{ asset('css/app.css') }}"></head><body><div class="shell"><aside><div class="brand"><b>KB</b><span><strong>Klinik Bustari</strong><small>Cash Flow</small></span></div><nav><a class="{{ request()->routeIs('dashboard')?'on':'' }}" href="{{ route('dashboard') }}">▦ Dashboard</a><a class="{{ request()->is('transactions/sale')?'on':'' }}" href="{{ route('transactions.index','sale') }}">▣ Cash Sales</a><a class="{{ request()->is('transactions/expense')?'on':'' }}" href="{{ route('transactions.index','expense') }}">▤ Expenses</a><a class="{{ request()->is('transactions/transfer')?'on':'' }}" href="{{ route('transactions.index','transfer') }}">↗ Transfers</a>@if(auth()->user()->isAdmin())<a class="{{ request()->routeIs('users.*')?'on':'' }}" href="{{ route('users.index') }}">♙ Users</a>@endif</nav><div class="account"><strong>{{ auth()->user()->name }}</strong><small>{{ ucfirst(auth()->user()->role) }}</small><form method="post" action="{{ route('logout') }}">@csrf<button>Sign out</button></form></div></aside><main><header><div><p class="eyebrow">KLINIK BUSTARI FINANCE</p><h1>@yield('heading')</h1><p class="muted">@yield('subheading')</p></div>@yield('actions')</header>@if(session('success'))<div class="flash ok">{{ session('success') }}</div>@endif @if($errors->any())<div class="flash bad">{{ $errors->first() }}</div>@endif @yield('content')</main></div>@yield('scripts')</body></html>
