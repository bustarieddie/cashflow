<!doctype html><html><head><meta name="viewport" content="width=device-width,initial-scale=1"><link rel="stylesheet" href="{{ asset('css/app.css') }}"><title>Verify code</title></head><body class="auth-page"><section class="auth-card"><div class="auth-logo">KB</div><p class="eyebrow">EMAIL VERIFICATION</p><h1>Enter login code</h1><p class="muted">Code sent to <strong>{{ $email }}</strong></p>@if(session('status'))<div class="flash ok">{{ session('status') }}</div>@endif @if($errors->any())<div class="flash bad">{{ $errors->first() }}</div>@endif<form method="post" action="{{ route('otp.check') }}">@csrf<label>6-digit code<input class="otp" inputmode="numeric" autocomplete="one-time-code" maxlength="6" name="code" required autofocus></label><button class="primary wide">Verify and sign in</button></form><a href="{{ route('login') }}">Use another email</a></section></body></html>
